-- =========================================================================
--  مركز تمكين — إضافة v8
--  إنشاء حساب «مسؤول التسجيل والقبول» (REG102) كحساب إداري حقيقي في Supabase.
--
--  لماذا؟ حتى يستطيع مسؤول القبول تحويل المتقدّم المقبول إلى حساب طالب رسمي
--  (عبر admin_create_user) ويعمل مفتاح الطالب وكلمة المرور فعليًا عند الدخول.
--
--  يُعرَّف القسم management_section = 'admissions' فتعرض له المنظومة
--  «لوحة التسجيل والقبول» بدل لوحة الإدارة العامة.
--
--  شغّله مرة واحدة في:  Supabase → SQL Editor → New query → Run
--  آمن لإعادة التشغيل (upsert).  يفترض أنك شغّلت setup.sql من قبل.
--
--  بيانات الدخول:  المفتاح REG102  ·  كلمة المرور 1234   (من بوابة «إداري»)
-- =========================================================================

select public.seed_user(
  'REG102',                 -- مفتاح الدخول
  '1234',                   -- كلمة المرور
  'management',             -- الدور (إداري) — لتفعيل صلاحيات is_staff
  'إدارة التسجيل والقبول',  -- الاسم
  'admissions',             -- القسم → يوجّه إلى لوحة التسجيل والقبول
  null, null, null,
  'admissions@tamkeen.edu'  -- بريد
);

-- =====================================================================
--  تم. سجّل الخروج ثم ادخل من بوابة «إداري» بالمفتاح REG102 وكلمة المرور 1234.
-- =====================================================================
