{
  "name": "tamkeen-center",
  "version": "1.0.0",
  "description": "منظومة مركز تمكين التعليمية — موقع ثابت (HTML + React عبر المتصفح) متصل بـ Supabase",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-env.js",
    "predev": "node scripts/generate-env.js || true",
    "dev": "serve . -l 8000",
    "start": "serve . -l 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
